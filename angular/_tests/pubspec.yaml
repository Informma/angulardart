name: _tests
description: Tests for AngularDart.
environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  angulardart: ^9.0.7
  angulardart_compiler: ^5.0.3
  angulardart_test: ^6.0.2
  build: ^4.0.0
  build_test: ^3.0.0
  glob: ^2.0.0
  js: ^0.7.0
  logging: ^1.0.0

  # Actual valid dependencies required.
  collection: ^1.15.0
  meta: ^1.3.0
  test: ^1.16.0
  web: '>=1.0.0 <1.2.0'

dev_dependencies:
  angulardart_forms: ^6.0.2

  # Actual valid dependencies required.
  analyzer: ^13.0.0
  build_runner: ^2.16.0
  build_web_compilers: ^4.8.0

  mockito: ^5.0.0
  package_config: ^2.0.0
  lints: ^6.0.0
  source_gen: ^4.0.0
  source_span: ^1.10.0
  term_glyph: ^1.2.0

dependency_overrides:
  web: '>=1.0.0 <1.2.0'
